name: test_temp_workflow

on:
  pull_request:
    types:
      - opened
      - synchronize
    # - reopened
      - closed

  push:
    branches:
      - main

jobs:
  job1:
    name: Testing workflow job1 temp
    runs-on: ubuntu-latest
    steps:
      - uses: Rippling/rippling-protos/.github/workflows/test_base.yml
